# Code Café – Intro to Web Dev & React ☕💻

This repository contains the starter code for the **Code Café: Intro to Web Dev & React** session.

The session is **hands-on** and designed for **all levels**.  
You’ll choose **one path** and work at your own pace.

You do NOT need to finish everything.

Make use of online resources, your peers and Code Cafe Leads

---

## How this repo is organised

```text
/
├─ web-dev-starter/               # HTML & CSS starter
├─ code-cafe-react-starter/       # React starter
└─ README.md                      # You are here
```

## Path 1: Build from scratch (HTML/CSS)
Use: `web-starter` 

Navigate to the folder in the terminal: 
```
 cd web-starter
 ```

Core (pick 2–3):
* Change name + bio + tags
* Add a  “Projects” with 2 project cards
* Add a navigation bar with 3 links (Home / Projects / Contact)
* Make it mobile-friendly (media queries)

Stretch:
* Add hover effects to buttons
* Use Flexbox for layouts
* Add a Contact form (doesn’t need to submit)

## Path 2: Improve a project/assignment
Use: `web-starter` (or your own project if you prefer)

Navigate to the folder in the terminal: 
```
 cd web-starter
 ```

Core:
* Improve spacing (padding/margins)
* Use consistent font sizes
* Add a color palette (2–3 colors max)
* Make layout responsive (mobile)
* Add a “hero” section (with headline + subtitle)

Stretch:
* Add a dark mode toggle (CSS class)
* Add simple JavaScript interaction (e.g. button changes text)

## Path 3: React / Get help
Use: `code-cafe-react-starter` 

Navigate to the folder in the terminal: 
```
 code-cafe-react-starter
 ```

Core:
* Create a Card component and reuse it at least 3 times
* Add state (`useState`) to track a counter OR  input
* Render a list using `.map()`

Stretch:
* Filter a list (search input)
* Fetch data (optional: loading + error states)
* Convert to TypeScript (only if you want)


# Getting started (important)

## Option 1: Use this repo with GitHub (recommended)

### Step 1: Fork the repo
1. Click the **Fork** button (top right)
2. Fork to **your own GitHub account**
3. Clone your fork to your computer

```bash
git clone https://github.com/YOUR-USERNAME/web-dev-starter.git
```
4. Open the folder in VS Code
5. Open the terminal (Terminal → New Terminal)
6. `cd` into the starter folder for your chosen path
7. Follow the README inside that folder

## Option 2: Download a zip file

### Step 1: Downlod and open folder
1. Click Code → Download ZIP
2. Unzip the folder
3. Open the folder in VS Code


3. Open the terminal (Terminal → New Terminal)
4. `cd` into the starter folder for your chosen path
5. Follow the README inside that folder

Tip: `cd` means “change directory”